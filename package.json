{
  "name": "letterboxd-contribution-graph",
  "version": "1.0.0",
  "description": "GitHub Action that generates a GitHub-style contribution graph from Letterboxd viewing history",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && ncc build lib/index.js -o dist",
    "test": "jest",
    "prepare": "npm run build"
  },
  "keywords": ["github", "action", "letterboxd", "contribution", "graph"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "cheerio": "^1.0.0-rc.12",
    "node-fetch": "^3.3.1"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^18.15.11",
    "@vercel/ncc": "^0.36.1",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.4"
  }
}

